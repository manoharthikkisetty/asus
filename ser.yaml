apiVersion: v1
kind: Service
metadata:
  name: sai
  labels:
    name: toomuch
spec:
  type: clusterIP
  selector:
    name: toomuch
  ports:
    - nodePort: 32334
      port: 8080
      targetPort: 8080
         