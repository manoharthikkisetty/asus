apiVersion: batch/v1
kind: CronJob
metadata:
  name: acf
  labels: 
    name: acflabel.yaml
spec:
  selector:
    matchLabels: pavan
    template:
      metadata:
        name: kalayan
        labels: 
          name: kalayanlabel.yaml 
      spec:
        containers:
          - name: babu
            image: nginx:1.25    
            ports:
              - containerPort: 80
                protocol: TCP 